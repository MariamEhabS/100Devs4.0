---
import { SITE } from '~/utils/config';
import { Image } from 'astro:assets';

---

<span class="self-top  font-bold text-gray-900 whitespace-nowrap ">
  <Image 
  class=" " 
  src={`${SITE.logo}`} 
  alt="#100Devs Logo" 
  decoding="async"
  loading="lazy"
  width={152}
  height={32}
/>
</span>
